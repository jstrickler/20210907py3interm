.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Worm Salad, run this command in your terminal:

.. code-block:: console

    $ pip install worm_salad

This is the preferred method to install Worm Salad, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Worm Salad can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/jstrickler/worm_salad

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/jstrickler/worm_salad/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/jstrickler/worm_salad
.. _tarball: https://github.com/jstrickler/worm_salad/tarball/master
